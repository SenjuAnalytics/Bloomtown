namespace Bloomtown.Shared.World;

/// <summary>
/// Broad in-game time bands used for ambient village flavor.
/// </summary>
public enum GameTimeOfDay : byte
{
    Morning = 0,
    Afternoon = 1,
    Evening = 2,
    Night = 3,
}